import { quicksand } from "@/app/fonts";

const OurImpact = () => {
  return (
    <section className="min-h-screen py-10 px-5 bg-background-yellow">
      <h1 className={`${quicksand.className} text-5xl text-background-green`}>
        Our Impact
      </h1>
    </section>
  );
};

export default OurImpact;
